name: calpedelapp
description: The official app of calpedel.it.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  firebase_core: ^0.5.0
  firebase_auth: ^0.18.0+1
  provider: ^4.3.2+2
  carousel_pro: ^1.0.0
  flutter:
    sdk: flutter
  mailer: ^3.2.1
  url_launcher: ^5.7.10
  get_it: ^5.0.1
  lint: ^1.3.0
  # firebase_ml_vision: ^0.9.7
  image_picker: ^0.6.7+11

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.0
#  lit_firebase_auth:
#    path: /Users/gordon/Documents/Fun with Flutter/projects/lit_firebase_auth
  animations: ^1.1.2
  google_fonts: ^1.1.0
  font_awesome_flutter: ^8.8.1



dev_dependencies:
  http: ^0.12.0
  flutter_launcher_icons: ^0.8.1
  flutter_test:
    sdk: flutter

flutter_launcher_icons:
  path: ../

flutter_icons:
  image_path: "images/icon.png"
  android: true
  ios: true


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - images/FiloGiardinaggio_1_6_30.jpg
    - images/PennaTouch4Funzioni.jpg
    - images/TermometroDigitaleAlimentiAllarme.jpg
    - images/Products/Components.jpg
    - images/Products/TVr.png
    - images/Products/Telefono.png
    - images/Products/Laptop.png
    - images/Products/Audio.png
    - images/Products/Camera.png
    - images/Products/elettrodomestici.png
    - images/Products/Tools.png
    - images/Products/Cables.png
    - images/Products/Alarm system.png
    - images/Products/Stationery.png
    - images/Products/Bike.png
    - images/PennaTouch4Funzioni.jpg
    - images/Decalc.jpg
    - images/Lamp.jpg
    - images/ShippingRules.png
    - images/SupportoSmartphone.JPG
    - icon/icon.png
    - images/calpedel.jpg
    - images/glass.jpg
    - images/glass2.jpg
    - images/2.jpg
    - images/3.jpg
    - images/1.jpg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
